# resume_hugo_static
this is sample resume static website made by hugo and will be use to host it on aws 


Run:
```
hugo server

```

If everything is fine, you’ll see something like:

Web Server is available at http://localhost:1313/


👉 Open that URL in the browser
If you’re using GitHub Codespaces, it will auto-forward the port.